using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    public AudioClip audioClip;
    // Start is called before the first frame update
    void Start()
    {
        //int length = audioClip.samples * audioClip.channels;

        //Debug.Log("Samples: " + audioClip.samples + ", Channels: " + audioClip.channels);

        //float[] data = new float[length];

        //audioClip.GetData(data, 0);

        ////for (int i = 0; i < 100 && i < data.Length; i++)
        ////{
        ////    Debug.Log("Sample " + i + ": " + data[i]);
        ////}

        //Debug.Log("Length of data: " + data.Length);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
